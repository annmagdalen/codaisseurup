<%= form_for [@event, @event.registrations.new] do |f| %>
  <p><strong>Event Price:</strong> <span>€</span> <%= @event.price %></p>

  <hr>


    <label>Number of Guests</label>
      <div class="form-group">
      <%= f.select :guest_count, [["1",1], ["2", 2], ["30", 30]], prompt: "Select the number of guests", class: "form-control" %>
    </div>


  <hr>

  <div class="actions">
    <%= f.submit "Register Now", class: "btn btn-success" %>
  </div>
<% end %>
